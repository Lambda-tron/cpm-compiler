main(): int {
  volatile n : int
  read(n)

  volatile i : int := 2
  arr : int[] := int[3,5,6,7]
  num  : int := arr[2]
  num := arr.length
  if ( (n / i) * i = n ) {
    print(2)
      for (i := 2, i <= n, i := i + 1) {

        print(n)
      }
    print(1)
  }
  return 0
}